type Project {
  id: Int!
  name: String!
  description: String!
  image: Image!
}

type Image {
  id: Int
  url: String
  height: Int
  width: Int
}

type Query {
  projects: [Project!]!
}

type Mutation {
  seedProjects: Boolean
  deleteAll: Boolean
}
